import { IronHttpInterceptor } from './iron-http-interceptor';

describe('IronHttpInterceptor', () => {
  it('should create an instance', () => {
    expect(new IronHttpInterceptor()).toBeTruthy();
  });
});
